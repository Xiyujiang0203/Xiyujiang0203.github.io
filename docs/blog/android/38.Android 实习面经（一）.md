---
createTime: 2024/09/03 23:08:18
tags:
  - Android
  - 高级
permalink: /blog/android/38/
title: Android 实习面经（一）
---
# Android 实习面经（一）

## 蔚来Android实习面经

一面（2024/3/11 35min）

1. 自我介绍

2. 聊我的本专业

3. 说一说MVP架构，MVVM架构
    <!-- image: removed (asset not available) -->

   MVP：V层持有P层，用户点击View，把数据发给P层，P层持有M层，然后P层把V层的数据发给M层获取其他数据，最后M层获取完数据后把数据还给P层，更新V层。P层也有V层的引用。

MVVM：V层持有VM层引用，对数据的监听来更新UI，VM持有Repository层引用，提供逻辑处理，Repository负责数据获取。

1. MVVM架构中ViewModel为什么在翻转屏幕之后还能保持原来的界面

ViewModelProvider通过ViewModelStore获取ViewModel
若获取失败，则通过ViewModelProvider.Factory创建ViewModel

1. 说一说Handler机制，Handler机制的使用场景
   工作原理：
    <!-- image: removed (asset not available) -->

    <!-- image: removed (asset not available) -->

类比：子线程给主线程送消息，主线程不定期检查“邮箱”
机制：

- 当一个线程需要使用Handler来处理消息时，首先需要创建一个Looper对象，并调用其prepare()方法创建一个与当前线程关联的消息队列。

- 接着，通过Looper的loop()方法启动消息循环，开始循环读取消息队列中的消息。

- 当有消息通过Handler的sendMessage()方法发送到消息队列时，Looper会不断从消息队列中取出消息，并将其传递给对应的Handler进行处理。

- Handler在接收到消息后，会调用自己的handleMessage()方法来处理消息。
  使用场景： UI更新，后台任务处理，定时任务，线程间通信。

6. Looper 与线程是如何保证一对一的？

通过 ThreadLocal 实现：每个线程的 ThreadLocal 中最多保存一个 Looper；若已存在则复用，否则创建新的。一个线程可以有多个 Handler，它们共享同一个 Looper。

7.Glide的三级缓存？三级缓存分别是什么？
活动缓存，页面看得到的图片，页面销毁则消失
内存缓存，应用关闭则消失
磁盘缓存，存储在手机中
网络缓存（不算缓存）

8.OkHttp的拦截器？
拦截器是责任链模式，首先有抽象接口，然后定义各个角色，最后进行组装。

- RetryAndFollowUpInterceptor：重试拦截器，判断任务要不要重试，超过最大次数返回错误，否则重试。

- BridgeInterceptor：请求拦截器，做公共请求头

- CacheInterceptor：缓存拦截器，看有没有缓存过

- ConnectInterceptor：连接拦截器，从之前的长连接中获取数据

- 执行任务拦截器

- 回调结果通知业务层

- 自定义拦截器
