---
title: "Bottom Navigation: ViewPager + BottomNavigationView + Fragment"
createTime: 2024/01/01 00:00:00
tags: [Android, Intermediate]
permalink: /en/blog/android/13/
---
# Bottom Navigation: ViewPager + BottomNavigationView + Fragment

## 1. Result: switch fragments by swiping or tapping the bottom navigation bar

&lt;div style="text-align:center;"&gt;&lt;img alt="" height="355" src="https://i-blog.csdnimg.cn/blog_migrate/017b34a17297976561297d5a10003539.png" width="187"&gt;&lt;/div&gt;

&lt;div style="text-align:center;"&gt;&lt;img alt="" height="377" src="https://i-blog.csdnimg.cn/blog_migrate/e0922f627458bbbb9a3bcfe8d638b8ea.png" width="187"&gt;&nbsp;&lt;/div&gt;

---

## 2. Implementation Steps

### 1. Write the layout

```
<!-- Inside a RelativeLayout -->
<androidx.viewpager.widget.ViewPager
        android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/bottom_navigation_view"
        />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:itemIconTint="@color/color"
        app:menu="@menu/item"
        app:itemTextColor="@color/color"
        />
```

### 2. Write menu items in res/menu/item.xml

Note: use self-explanatory icon names

```
<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/message"
        android:icon="@drawable/xinxi"
        android:title="Messages">
    </item>

    <item
        android:id="@+id/contact"
        android:icon="@drawable/contact"
        android:title="Contacts">
    </item>

    <item
        android:id="@+id/dong_tai"
        android:icon="@drawable/dongtai"
        android:title="Feed">
    </item>

</menu>
```

### 3. Write fragment code and layouts — see details at

[https://blog.csdn.net/qq_61735602/article/details/124531829?spm=1001.2014.3001.5502](https://blog.csdn.net/qq_61735602/article/details/124531829?spm=1001.2014.3001.5502) 

### Android Fragment Study Notes

### 4. Get the ViewPager control and write an adapter to pass view data

```
private ViewPager viewPager;
private List<View> views = new ArrayList<>();
viewPager = (ViewPager) findViewById(R.id.view_pager);
//Inflate fragments as views
views.add(View.inflate(MainActivity.this,R.layout.first_message_fragment,null));
        views.add(View.inflate(MainActivity.this,R.layout.second_contact_fragment,null));
        views.add(View.inflate(MainActivity.this,R.layout.third_dong_tai_fragment,null));
  

 viewPager.setAdapter(new PagerAdapter() {
            @Override
            public int getCount() {//Number of pages in the ViewPager
                return views.size();
            }

            @Override
            public boolean isViewFromObject(@NonNull View view, @NonNull Object object) {
                return view == object;
            }

            @NonNull
            @Override//Called when this page is shown
            public Object instantiateItem(@NonNull ViewGroup container, int position) {
                container.addView(views.get(position));
                return views.get(position);
            }

            @Override
            public void destroyItem(@NonNull ViewGroup container, int position, @NonNull Object object) {//Called when this page is off-screen
                container.removeView((View) object);
            }
        });
```

### 5. Bind ViewPager to the bottom navigation bar so the bar updates when swiping

```
 viewPager.addOnPageChangeListener(new ViewPager.OnPageChangeListener() {
            @Override
            public void onPageScrolled(int position, float positionOffset, int positionOffsetPixels) {

            }

            @Override//Called when a page is selected
            public void onPageSelected(int position) {
                switch (position){//Select the corresponding bottom nav item by page index
                    case 0:
                        bottomNavigationView.setSelectedItemId(R.id.message);
                        break;
                    case 1:
                        bottomNavigationView.setSelectedItemId(R.id.contact);
                        break;
                    case 2:
                        bottomNavigationView.setSelectedItemId(R.id.dong_tai);
                        break;
                }
            }

            @Override
            public void onPageScrollStateChanged(int state) {

            }
        });
```

### 6. Bind the bottom navigation bar to the ViewPager

```
 bottomNavigationView.setOnItemSelectedListener(new NavigationBarView.OnItemSelectedListener() {
            @Override
            public boolean onNavigationItemSelected(@NonNull MenuItem item) {
                switch (item.getItemId()){//Select the page by bottom nav item id
                    case R.id.message:
                        viewPager.setCurrentItem(0);
                        break;
                    case R.id.contact:
                        viewPager.setCurrentItem(1);
                        break;
                    case R.id.dong_tai:
                        viewPager.setCurrentItem(2);
                        break;
                }

                return true;
            }
        });
```

The above achieves the paging effect shown in the images at the top.

---

Published on: 2023-11-28 15:11:08

Blog: https://jixiuy.blog.csdn.net
