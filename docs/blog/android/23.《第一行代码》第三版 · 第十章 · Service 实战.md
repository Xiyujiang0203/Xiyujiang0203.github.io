---
createTime: 2025/10/16 15:51:12
tags:
  - Android
  - 中级
permalink: /blog/android/23/
title: 《第一行代码》第三版 · 第十章 · Service 实战
---
# 《第一行代码》第三版 · 第十章 · Service 实战

## 10 服务

### 10.1 服务是什么

服务：实现程序后台运行的解决方案，使用执行不需要和用户交互还需要长期运行的任务。

### 10.2 多线程

#### 10.2.1 线程的基本用法

如：网络请求如果在主线程中进行会阻塞线程影响用户体验

方法一：

```
class MyThread : Thread(){
	override fun run(){
	
	}
}
//使用
MyThread().start()
```

方法二：

```
class MyThread : Runnable{
	override fun run(){
		
	}
}

val myThread = MyThread()
Thread(myThread).start()
```

方法三：

```
Thread{

}.start()
```

方法四：kotlin中

```
thread{
	
}
```

#### 10.2.2 在子线程里更新 UI

注：子线程里不能更新UI必须异步处理

例子：通过点击改变textview的内容

```
package com.android.androidthreadtest

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.os.Handler
import android.os.Message
import kotlinx.android.synthetic.main.activity_main.*
import kotlin.concurrent.thread

class MainActivity : AppCompatActivity() {

    val updateText = 1

    val handler = object : Handler(){
        override fun handleMessage(msg: Message) {
            super.handleMessage(msg)

            when (msg.what) {
                updateText -> textView.text = "Nice to meet you"
            }
        }
    }

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
        changeTextBtn.setOnClickListener {
            thread {
                val msg = Message()
                msg.what = updateText
                handler.sendMessage(msg)
            }
        }
    }
}
```

#### 10.2.3 解析异步消息处理机制

通过子线程发送Message到MessageQueue里，Looper会取出消息传递到handleMessage主线程中处理

#### 10.2.4 使用 AsyncTask

废弃，不推荐使用

```
package com.android.androidthreadtest

import android.os.AsyncTask
import android.widget.Toast
import java.lang.Exception

/**
 * 第一个参数表示执行AsyncTask需要传入的参数
 * 第二个参数表示后台任务执行时，如界面需要显示当前进度，这里指定泛型作为进度单位
 * 第三个参数表示任务执行完，需要对结果进行返回
 * 这里不需要传入参数给后台
 * 利用整型作为进度单位
 * 布尔值来反馈执行结果
 */
class DownloadTask : AsyncTask<Unit, Int, Boolean>() {

    //后台任务开始前调用，用于界面上的初始化操作，如：显示一个对话框
    override fun onPreExecute() {
        super.onPreExecute()
        progressDialog.show() //显示进度对话框
    }
    
    //子线程中运行，完成后return返回结果，publishProgress传参给主线程，调用主线程方法
    override fun doInBackground(vararg p0: Unit?) = try{
        while (true) {
            val downloadPercent = doDownload()
            publishProgress(downloadPercent)
            if (downloadPercent >= 100) {
                break
            }
        }
        true
    }catch (e:Exception){
        false
    } 

    
    //return后调用
    override fun onPostExecute(result: Boolean) {
        super.onPostExecute(result)
        //提示下载结果
        if (result) {
            Toast.makeText(context,"Download succeeded",Toast.LENGTH_SHORT).show()
        }else  Toast.makeText(context,"Download failed",Toast.LENGTH_SHORT).show()
    }
    //主线程方法
    override fun onProgressUpdate(vararg values: Int?) {
        super.onProgressUpdate(*values)
        
        //更新下载进度
        progressDialog.setMessage("Downloaded ${values[0]%}")
    }
}
```

### 10.3 服务

#### 10.3.1 定义服务

1. new 一个服务

2. 重写方法

```
class MyService : Service() {

    override fun onBind(intent: Intent): IBinder {
        TODO("Return the communication channel to the service.")
    }

    //创建的时候执行
    override fun onCreate() {
        super.onCreate()
    }

    //启动的时候执行:
    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
        return super.onStartCommand(intent, flags, startId)
    }

    //销毁的时候执行,回收不使用的资源
    override fun onDestroy() {
        super.onDestroy()
    }
}
```

注：只有第一次打开的时候会创建，之后都是打开

#### 10.3.2 启动和停止服务

```
startServiceBtn.setOnClickListener {
            val intent = Intent(this, MyService::class.java)
            startService(intent)
        }
        stopServiceBtn.setOnClickListener {
            val intent = Intent(this, MyService::class.java)
            stopService(intent)
        }
```

```
//创建的时候执行
override fun onCreate() {
    super.onCreate()
    Log.d(TAG, "onCreate: ")
}

//启动的时候执行:
override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
    Log.d(TAG, "onStartCommand: ")
    return super.onStartCommand(intent, flags, startId)
}

//销毁的时候执行,回收不使用的资源
override fun onDestroy() {
    super.onDestroy()
    Log.d(TAG, "onDestroy: ")
}
```

注：安卓8.0后，应用的后台功能被大幅削减，之后应用前台可见的时候不会被回收，后台中随时可能被回收。

#### 10.3.3 Activity 和 Service 通信

1. 写实现类，写要公开的方法，返回

```
private  val mBinder =  DownloadBinder()

class DownloadBinder : Binder() {
    fun startDownload(){
        Log.d("MyService", "startDownload: ")
    }

    fun getProgress(): Int{
        Log.d("MyService", "getProgress: ")
        return 0
    }
}

override fun onBind(intent: Intent): IBinder {
    return mBinder
}
```

1. 创建匿名内部类对象，重写方法使用传来的方法。用对象绑定服务。

```
lateinit var downloadBinder: MyService.DownloadBinder

private val connection = object : ServiceConnection{
    //活动和服务创建链接
    override fun onServiceConnected(p0: ComponentName?, p1: IBinder?) {
        downloadBinder = p1 as MyService.DownloadBinder
        downloadBinder.startDownload()
        downloadBinder.getProgress()
    }

    //进程崩溃或者被杀掉
    override fun onServiceDisconnected(p0: ComponentName?) {
        TODO("Not yet implemented")
    }
}

  bindServiceBtn.setOnClickListener {
            val intent = Intent(this, MyService::class.java)
            bindService(intent,connection, Context.BIND_AUTO_CREATE)//第三个参数表示绑定后自动创建服务
        }
        unbindService(connection)
```

### 10.4 服务的声明周期

- 创建只会执行一次，启动服务会一直执行但只有一个服务实例，关一次就关掉了

- stopSelf()服务内部关闭

- onBind 是 bindService 的回调

- 如果同时创建和绑定服务，那么必须两者都取消才能销毁服务

### 10.5 服务的更多技巧

#### 10.5.1 前台服务

- 在状态栏有个图标，下拉状态栏可以看到更详细的信息

1. 权限

```
<uses-permission android:name="android.permission.FOREGROUND_SERVICE"></uses-permission>
```

1. 创建前台服务

```
override fun onCreate() {
        super.onCreate()
        val intent = Intent(this, MainActivity::class.java)
        val pi = PendingIntent.getActivity(this,0,intent,PendingIntent.FLAG_IMMUTABLE)
        val notification = NotificationCompat.Builder(this, "my_service")
            .setContentText("This is content text")
            .setContentTitle("This is content title")
            .setSmallIcon(R.drawable.small_icon)
            .setLargeIcon(BitmapFactory.decodeResource(resources, R.drawable.large_icon))
            .setContentIntent(pi)
            .build()
        startForeground(1,notification)
    }
```

#### 10.5.2 使用IntentService(不推荐)

服务代码是在主线程中，如果处理耗时逻辑会出现ANR情况

标准的写法应该在每个方法里开个子线程

```
//启动的时候执行:
    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
        Log.d(TAG, "onStartCommand: ")
        thread{
            //具体逻辑
            //逻辑执行完后停止服务
            stopSelf()
        }
        return super.onStartCommand(intent, flags, startId)
    }
```

- 这种异步的会停止的服务就是IntentService,可以当成服务来用

```
class MyIntentService : IntentService("MyIntentService") {
    override fun onHandleIntent(p0: Intent?) {
        Log.d(TAG, "Thread id is ${Thread.currentThread().name}")
    }
    private val TAG = "MyIntentService"

    override fun onDestroy() {
        super.onDestroy()
        Log.d(TAG, "onDestroy: ")
    }
}

 startIntentServiceBtn.setOnClickListener {
            Log.d(TAG, "Thread id is ${Thread.currentThread().name}")
            val intent = Intent(this, MyIntentService::class.java)
            startService(intent)
        }
```
