---
createTime: 2024/01/01 00:00:00
tags:
  - Android
  - Expert
permalink: /en/blog/android/36/
title: Android-BluetoothKit Integration and Gradle Troubleshooting
---
# Android-BluetoothKit Integration and Gradle Troubleshooting

1. Modify the project `build.gradle`:

```xml
// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.3.0'
        classpath 'com.jfrog.bintray.gradle:gradle-bintray-plugin:1.0'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

```

1. Modify the Module `build.gradle`

```xml
apply plugin: 'com.android.application'

android {
    compileSdkVersion 30
    buildToolsVersion '25.0.0'

    defaultConfig {
        applicationId "com.inuker.bluetooth"
        minSdkVersion 28
        targetSdkVersion 30
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    sourceSets {
        main {
            jniLibs.srcDirs = ['libs']
        }
    }
}

dependencies {
    implementation 'com.inuker.bluetooth:library:1.4.0'
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation 'com.android.support:appcompat-v7:23.1.0'
    implementation 'com.android.support:design:23.1.0'
    //    compile 'com.inuker.bluetooth:library:1.3.8'
    implementation project(path: ':library')
}

```

1. Modify `library/build.gradle`

```xml
// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.3.0'
        classpath 'com.jfrog.bintray.gradle:gradle-bintray-plugin:1.0'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
```

1. Change the Gradle version
    <!-- image: removed (asset not available) -->

2. Find `CharacterActivity.java` and comment out this code
    <!-- image: removed (asset not available) -->

3. Start the emulator

 <!-- image: removed (asset not available) -->

8. On a real device, enable system Bluetooth and location, and grant the app background Bluetooth and location permissions

---

Published: 2026-04-19 09:02:50

Blog: https://jixiuy.blog.csdn.net
