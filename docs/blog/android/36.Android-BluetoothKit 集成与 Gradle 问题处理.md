---
createTime: 2024/01/01 00:00:00
tags:
  - Android
  - 高级
permalink: /blog/android/36/
title: Android-BluetoothKit 集成与 Gradle 问题处理
---
# Android-BluetoothKit 集成与 Gradle 问题处理

1. 修改项目 build-gradle，修改后的代码：

```xml
// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.3.0'
        classpath 'com.jfrog.bintray.gradle:gradle-bintray-plugin:1.0'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

```

1. 修改Module 的build-gradle

```xml
apply plugin: 'com.android.application'

android {
    compileSdkVersion 30
    buildToolsVersion '25.0.0'

    defaultConfig {
        applicationId "com.inuker.bluetooth"
        minSdkVersion 28
        targetSdkVersion 30
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    sourceSets {
        main {
            jniLibs.srcDirs = ['libs']
        }
    }
}

dependencies {
    implementation 'com.inuker.bluetooth:library:1.4.0'
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation 'com.android.support:appcompat-v7:23.1.0'
    implementation 'com.android.support:design:23.1.0'
    //    compile 'com.inuker.bluetooth:library:1.3.8'
    implementation project(path: ':library')
}

```

1. 修改 library/build.gradle

```xml
// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.3.0'
        classpath 'com.jfrog.bintray.gradle:gradle-bintray-plugin:1.0'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
```

1. 修改 Gradle的版本
    <!-- image: removed (asset not available) -->

2. 找到CharacterActivity.java，注释掉这个代码
    <!-- image: removed (asset not available) -->

3. 启动虚拟机

 <!-- image: removed (asset not available) -->

8. 如果是在真机上使用，需要打开系统蓝牙和定位功能和app后台蓝牙和定位权限

---

发布于：2026-04-19 09:02:50

博客：https://jixiuy.blog.csdn.net
