---
createTime: 2022/08/19 22:48:35
tags:
  - Android
  - Intermediate
permalink: /en/blog/android/19/
title: First Line of Code (3rd Ed.) - Chapter 9 - Multimedia
---
# First Line of Code (3rd Ed.) - Chapter 9 - Multimedia

## Chapter 9 Multimedia

### 9.1 Running the App on a Phone

Enable USB debugging and connect the phone

### 9.2 Using Notifications

#### 9.2.1 Creating a Notification Channel

Introduced in Android 8.0 — notification channels are the granular options in Settings

1. Get the NotificationManager

2. Create a channel with id, name, and importance

#### 9.2.2 Basic Notification Usage

- Notifications are usually created in broadcasts and services because they are needed when the app is in the background

1. Button to show notification; second Activity to open when tapped

2. Code

```
class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
        val manager = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager
        if(Build.VERSION.SDK_INT>= VERSION_CODES .O){
            val channel =
                NotificationChannel("normal", "Normal", NotificationManager.IMPORTANCE_DEFAULT)
            manager.createNotificationChannel(channel)
        }
        sendNotice.setOnClickListener {
            val intent = Intent(this, NotificationActivity::class.java)
            val pi = PendingIntent.getActivity(this,0,intent,PendingIntent.FLAG_IMMUTABLE)
            val notification = NotificationCompat.Builder(this, "normal")
                .setContentTitle("This is content title")
                .setContentText("This is content text")
                .setSmallIcon(R.drawable.small_icon)
                .setLargeIcon(BitmapFactory.decodeResource(resources, R.drawable.large_icon))
                .setContentIntent(pi)
                .setAutoCancel(true)
                .build()
            manager.notify(1,notification)
        }
    }
}
```

#### 9.2.3 Advanced Notification Tips

1. Long text — use setStyle instead of setContentText

```
 NotificationCompat.Builder(this, "normal")..setStyle(NotificationCompat.BigTextStyle().bigText("Learn how to build notifications, send and sync data,and " +
                        "use voice actions. Get the official Android IDE and developer tools to build apps for Android."))
                        //Long-press to show full text
```

1. Large image

```
.setStyle(NotificationCompat.BigPictureStyle().bigPicture(BitmapFactory.decodeResource(resources,R.drawable.big_image)))
```

1. Importance cannot be changed after the notification is created; users can change it in system settings

- Setting priority in First Line of Code had no effect

#### 9.3 Camera and Gallery

#### 9.3.1 Taking a Photo with the Camera

1. Button to open camera; ImageView to display

2. Logic — includes image orientation handling

```
class MainActivity : AppCompatActivity() {
    val takePhoto = 1
    lateinit var imageUri: Uri
    lateinit var outputImage: File

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
        takePhotoBtn.setOnClickListener {
            //Create file in cache dir to save photo
            outputImage = File(externalCacheDir,"output_image.jpg")
            if (outputImage.exists()) {
                outputImage.delete()
            }
            outputImage.createNewFile()
            imageUri = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.N) {
                FileProvider.getUriForFile(this,"com.example.cameraalbumtest.fileprovider",outputImage)
            }else{
                Uri.fromFile(outputImage)
            }
            val intent = Intent("android.media.action.IMAGE_CAPTURE")
            intent.putExtra(MediaStore.EXTRA_OUTPUT, imageUri)
            startActivityForResult(intent,takePhoto)
        }
    }

    override fun onActivityResult(requestCode: Int, resultCode: Int, data: Intent?) {
        super.onActivityResult(requestCode, resultCode, data)
        when (requestCode) {
            takePhoto ->{
                if (resultCode == Activity.RESULT_OK) {
                    val bitmap =
                        BitmapFactory.decodeStream(contentResolver.openInputStream(imageUri))
                    imageView.setImageBitmap(rotateIfRequired(bitmap))
                }
            }
        }
    }

    //Image orientation handling
    private fun rotateIfRequired(bitmap: Bitmap): Bitmap {
        val exif = ExifInterface(outputImage.path)
        val orientation =
            exif.getAttributeInt(ExifInterface.TAG_ORIENTATION, ExifInterface.ORIENTATION_NORMAL)
        return when (orientation) {
            ExifInterface.ORIENTATION_ROTATE_90 ->rotateBitmap(bitmap,90)
            ExifInterface.ORIENTATION_ROTATE_180 ->rotateBitmap(bitmap,180)
            ExifInterface.ORIENTATION_ROTATE_270 ->rotateBitmap(bitmap,270)
            else -> bitmap
        }
    }

    private fun rotateBitmap(bitmap: Bitmap, i: Int): Bitmap {
        val matrix = Matrix()
        matrix.postRotate(i.toFloat())
        val rotatedBitmap =
            Bitmap.createBitmap(bitmap, 0, 0, bitmap.width, bitmap.height, matrix, true)
        bitmap.recycle()
        return rotatedBitmap
    }
}
```

#### 9.3.2 Picking an Image from the Gallery

```
class MainActivity : AppCompatActivity() {
   
    val fromAlbum = 2
    

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
       
        fromAlbumBtn.setOnClickListener {
            val intent = Intent(Intent.ACTION_OPEN_DOCUMENT)
            intent.addCategory(Intent.CATEGORY_OPENABLE)
            intent.type = "image/*"
            startActivityForResult(intent,fromAlbum)
        }
    }

    override fun onActivityResult(requestCode: Int, resultCode: Int, data: Intent?) {
        super.onActivityResult(requestCode, resultCode, data)
        when (requestCode) {
          
            fromAlbum ->{

                if (resultCode == Activity.RESULT_OK && data != null) {

                    data.data?.let {
                        uri ->
                        val bitmap = getBitmapFromUri(uri)
                        imageView.setImageBitmap(bitmap)
                    }
                }
            }
        }
    }
    private val TAG = "MainActivity"
    private fun getBitmapFromUri(uri: Uri) = contentResolver.openFileDescriptor(uri,"r").use {
        BitmapFactory.decodeFileDescriptor(it!!.fileDescriptor)
    }
}
```

### 9.4 Playing Multimedia

#### 9.4.1 Playing Audio

1. Three buttons

2. Create assets under main and add an audio file

3. Logic

```
class MainActivity : AppCompatActivity() {

    private val mediaPlayer = MediaPlayer()

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
        initMediaPlayer()
        play.setOnClickListener {
            if (!mediaPlayer.isPlaying) {
                mediaPlayer.start()
            }
        }
        pause.setOnClickListener {
            if (mediaPlayer.isPlaying) {
                mediaPlayer.pause()
            }
        }
        stop.setOnClickListener {
            if (mediaPlayer.isPlaying) {
                mediaPlayer.reset()
                initMediaPlayer()
            }
        }
    }

    private fun initMediaPlayer() {
        val assetManager = assets
        val fd = assetManager.openFd("music.mp3")
        mediaPlayer.setDataSource(fd.fileDescriptor, fd.startOffset, fd.length)
        mediaPlayer.prepare()
    }

    override fun onDestroy() {
        super.onDestroy()
        mediaPlayer.stop()
        mediaPlayer.release()
    }
}
```

#### 9.4.2 Playing Video

1. Buttons and VideoView

```
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >

        <Button
            android:id="@+id/play"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Play" />

        <Button
            android:id="@+id/pause"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Pause" />

        <Button
            android:id="@+id/replay"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Replay" />

    </LinearLayout>

    <VideoView
        android:id="@+id/videoView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</LinearLayout>
```

1. Put video in res/raw

2. Logic

```
class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
        val uri = Uri.parse("android.resource://$packageName/${R.raw.video}")
        videoView.setVideoURI(uri)
        play.setOnClickListener {
            if (!videoView.isPlaying) {
                videoView.start()
            }
        }
        pause.setOnClickListener {
            if (videoView.isPlaying) {
                videoView.pause()
            }
        }
        replay.setOnClickListener {
            if (videoView.isPlaying) {
                videoView.resume()
            }
        }
    }

    override fun onDestroy() {
        super.onDestroy()
        videoView.suspend()
    }
}
```

### 9.5 Kotlin Classroom: infix Functions

Build key-value pairs like A to B

Purpose: remove computer syntax like dots and parentheses so calls read more like English

- Example 1

```
//Check if a string starts with a prefix
if("Hello Kotlin".startsWith("Hello")){
}
```

- Optimize with infix

1. Create infix.kt

2. ```
   infix fun String.beginsWith(prefix:String) = startsWith(prefix)
   ```

3. Usage

```
if("Hello Kotlin" beginsWith "Hello")){
}
```

- Requirements

1. Cannot be a top-level function — must be a member or extension on a class

2. Only one parameter

- Example 2

```
//Check if a collection contains an element
val list = listOf("Apple","Banana","Orange","Pear","Grape")
if(list.contains("Banana")){
}
```

- Optimize with infix

```
infix fun <T> Collection<T>.has(element: T) = contains(element)
/**
*Collection is the root interface; extension here is available on all subtypes
*Generic accepts any element type
```

- Usage

```
val list = listOf("Apple","Banana","Orange","Pear","Grape")
if(list has "Banana"){
}
```

- Source of A to B

```
public infix fun<A,B> A.to(that: B): Pair<A, B> = Pair(this, that)
```

- Implement A to B yourself

```
//in infix.kt
infix  fun<A,B> A.with(that: B): Pair<A, B> = Pair(this, that)

val map = mapOf("Apple" with 1)
```

### 9.6 Git: Version Control

1. In PlayVideoTest, init repo: git init

#### 9.6.1 Ignoring Files

Contents of .gitignore are excluded from version control; AS generates gitignore in root and app

- `*.iml` matches any .iml file

To exclude test files from version control:

```
/build
/src/test
/src/androidTest
```

- Commit

```
git add .
git commit -m "First commit."
```

#### 9.6.2 Viewing Changes

- git status

1. Add another log line

2. git status

3. git diff — view changes; q to quit, Enter to scroll

4. git diff app/src/main/java/com/android/playvideotest/MainActivity.kt — diff for one file

#### 9.6.3 Reverting Uncommitted Changes

- git checkout app/src/main/java/com/android/playvideotest/MainActivity.kt

Problem: cannot revert after git add

Fix: unstage first

- git reset HEAD app/src/main/java/com/android/playvideotest/MainActivity.kt

- Then revert the file changes

#### 9.6.4 Viewing Commit History

- git log — all commits: id, author, time, message

- git log 925d1274a0e562bba4e3d5becfb8d58d352a78da — one commit by id

- git log -1 — latest commit
