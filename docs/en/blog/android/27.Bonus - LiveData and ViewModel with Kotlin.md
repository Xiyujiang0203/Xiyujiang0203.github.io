---
title: "Bonus: LiveData and ViewModel with Kotlin"
createTime: 2022/10/16 14:06:24
tags: [Android, Advanced]
permalink: /en/blog/android/27/
---
# Bonus: LiveData and ViewModel with Kotlin

- Example: one label and two buttons — use Kotlin to increment/decrement the label by 1.

Demo:

&lt;div align="center" style="border: 3px solid gray;border-radius: 27px;overflow: hidden;"&gt; &lt;a class="link-info" href="https://live.csdn.net/v/embed/245677?autoplay=0" rel="nofollow" title="Bonus: Kotlin LiveData and ViewModel Example"&gt;Bonus: Kotlin LiveData and ViewModel Example&lt;/a&gt;&lt;iframe id="Ex44nArW-1665900313117" frameborder="0" src="https://live.csdn.net/v/embed/245677?autoplay=0" allowfullscreen="true" data-mediaembed="csdn" style="width: 100%; aspect-ratio: 2;" allow="fullscreen" loading="lazy"&gt;&lt;/iframe&gt;&lt;/div&gt;

Step 1: Add dependencies

- Find what you need on the official site: [LiveData dependency link](https://developer.android.google.cn/jetpack/androidx/releases/lifecycle?hl=zh-cn) 
   <!-- image: removed (asset not available) -->

   <!-- image: removed (asset not available) -->

Step 2: Layout has one label and two buttons
Step 3: Write a ViewModel to manage data

```kotlin
class MyViewModel : ViewModel() {

    //1 lazy init
    //val number:MutableLiveData<Int> by lazy { MutableLiveData<Int>().also { it.value = 0  } }
    //2 eager init
    //val number = MutableLiveData(0)

    //3 exposing MutableLiveData directly is bad
    //underscore prefix for internal variable
    private val _number:MutableLiveData<Int> by lazy { MutableLiveData<Int>().also { it.value=0 } }
    val number:LiveData<Int>
    get() = _number
    
    fun addScore(aNumber:Int){
        //only when non-null
        _number.value = _number.value?.plus(aNumber)
        //runs regardless of null
        //number.value = number.value!!.plus(aNumber)
    }
}
```

Step 4: Observe data changes in the Activity and update the layout

```kotlin
class MainActivity : AppCompatActivity() {

    private var good:Button?=null
    private var bad:Button?=null
    private var textView:TextView?=null

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        good = findViewById(R.id.good)
        bad = findViewById(R.id.bad)
        textView = findViewById(R.id.textView)
		
        val viewModel = ViewModelProvider(this)[MyViewModel::class.java]
        viewModel.number.observe(this) { textView?.text = it.toString() }

        good?.setOnClickListener {
            viewModel.addScore(1)
        }
        bad?.setOnClickListener {
            viewModel.addScore(-1)
        }
    }

}
```
